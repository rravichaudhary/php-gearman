<?php

require_once('autoload.php');
require_once('config.php');

$worker= new GearmanWorker();
$worker->addServer();
if(!empty($config)){
	foreach($config as $function=>$class){
		$worker->register($function, $class);
	}

}
while ($worker->work());

